# 固定变量设计

- 固定变量由平台完成，与模型底层无关

- 每个计算任务有自己单独的固定变量列表，存在XRUnit的固定变量列表里

- 为了确保在没有更新初值/修改固定变量的情况下计算一致性，固定变量的初值以XRUnit为准

- 因为上一条，如果修改固定值，需要把这个值写入XRUnit相关变量

- 平台在固定变量的界面里，如果固定了变量，则显示XRUnit里此变量固定值，如果没有，则把当前初始值加入

- 同上一条，如果固定变量了，固定变量的上下界显示为和初值一样