1.组分体系和Slate设置：单元模型的每个输入输出流股都指定Slate
需要添加：GetSlateNum, GetSlateIdOfPort, SetSlate(替换原来的setComponents)

2.热力学
①配置：设置一个默认的全局热力学，如果模型需要设置更复杂的热力学参数，则通过自身的parameter实现。
③热力学函数放在一个文件夹里，能够被其他的模型调用即可，平台不负责建热力学block，只是提供多种热力学函数接口（python, pyomo, c++）

3.模型描述接口
SetLanguage
变量描述（是否考虑中英文？）、单位，涉及调整模型接口函数

4.各类模型调用逻辑统一，编制说明书
setProblemType是否还保留？何时调用
是否对fixVariable的组合进行检查（在fixVar的时候检查）
PrepareRuntime的时机？【validate时、改fix variable，模型刚拉进去时、设置计算任务、流股连接时、从文件加载】PrepareRuntime后不应该自动求解？
fix Var时不再变量消失
如果模型能够拿到变量列表，并且变量列表不变化，就没事了

5.不同case的约定：流程图相同，共享初值、当前值，共享CU
但CU和UnitModel启用状态不共享，固定变量不同
一般来说RTO会定义两个case，一个是Simulation，一个是OnlineUse（整定和优化都用这个，然后通过overwrite bound的方式来调整固定变量）。

6.变量上下界：原始的上下界维护一个并可访问但界面不体现，变量fix时，上下界变为当前值，取消fix后变为原模型上下界
模型连接时上下界按上游，CU块连接按照CU里的变量上下界来
需要CU块中绑定变量上下界可以求解，新建CU中的绑定变量时上下界从绑定变量读取
Overwrite的上下界不显示只临时使用

7.RTS集成
①宏：区分初始化部分和在线执行部分，写成两个脚本给RTS调用，可传递输入参数（通过环境变量传递）
②RTS模块的封装改造
③相关RTS块的界面制作

其他：
1.单元模型界面
①精馏塔、反应器等定制界面。第三方模块用通用界面，内置模块逐渐替换为自己的界面。
②表格风格统一
小问题：流股信息显示表格有效位数、按Component定义顺序显示等
2.Python环境梳理，依赖项license情况统计，【给出Requirement，让AI检查一下】
3.打包&一键安装exe ，需要按照尺寸提供个图片
4*.动态模式，工程文件用动态模式打开，防止程序打架。

